#include <iostream>

using namespace std;

int main()
{
    int n;
    cout << "Enter a non-negative integer: ";
    cin >> n;
    //write any other statements here
    do {
    cout << "Enter a non-negative integer: ";
    if (bool value=true);
    cout << "The integer you entered is negative." << endl;
    else (bool value=false);
    cout << "Enter a non-negative integer: ";

    //write your answer here
 }  while (n < 0);
    return 0;
}
